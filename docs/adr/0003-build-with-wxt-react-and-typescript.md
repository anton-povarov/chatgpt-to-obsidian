# Build with WXT, React, and TypeScript

The extension will use WXT with React and TypeScript and target Chromium Manifest V3. WXT provides the popup, content-script, background-service-worker, messaging, and browser-build structure needed by the product while keeping browser APIs explicit; React supports the editable Web Clipper-style popup without requiring a bespoke UI layer.
